{
  "version": 2,
  "buildCommand": "cd ../packages/shared && npm install && npm run build && cd ../../server && npm install && npm run build",
  "builds": [
    {
      "src": "dist/index.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": "dist/**"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/dist/index.js" }
  ]
}
