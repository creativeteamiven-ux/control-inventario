{
  "version": 2,
  "buildCommand": "cd ../packages/shared && npm install && npm run build && cd ../../server && npm install && npm run build && node scripts/build-vercel-output.mjs",
  "outputDirectory": ".vercel/output"
}
