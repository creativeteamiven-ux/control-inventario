# ===========================================
# PRODUCCIÓN (Vercel + Neon)
# No subas este archivo con valores reales. Configura estas variables en:
# Vercel → Tu proyecto → Settings → Environment Variables
# Neon → Tu proyecto → Connection string (Pooled) para producción
# ===========================================
DATABASE_URL=
JWT_SECRET=
REFRESH_SECRET=
PORT=3001
# URL del frontend (dominio de producción). CORS permitirá este origen.
CLIENT_URL=https://thewarehouse.diosfuentedepoder.com
